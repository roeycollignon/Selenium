---
- hosts: all
  vars:
  - aaa2: "{{aaa|default('AA')}}"
  - bbb2: "{{bbb|default('BB')}}"
  - ccc2: "{{ccc|default('CC')}}"
  tasks:
  - name: PrintSomething
    debug: var=aaa2
  - name: PrintSomething
    debug: var1=bbb2
  - name: PrintSomething
    debug: var2=ccc2
